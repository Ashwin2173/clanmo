struct Point {
    x, y
}

function main() {
    var p = Point {
        x = Point {
            x = 1,
            y = 2
        },
        y = 3
    };
    print(str_point(p));
    return 0;
}

function str_point(p) {
    return "point { x=" + p.x + ", y=" + p.y + " }";
}